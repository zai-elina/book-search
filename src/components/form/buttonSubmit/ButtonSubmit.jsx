import React from 'react';
import classes from './ButtonSubmit.module.css'

const ButtonSubmit = () => {
    return (
        <button type="submit" className={classes.buttonSubmit}>Найти</button>
    );
};

export default ButtonSubmit;